<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.example.quizCoach.controller.ProfileController"
            prefWidth="1000" prefHeight="600"
            style="-fx-background-color: linear-gradient(to right, #0f3c63, #7bb1d1);">

    <!-- Top -->
    <top>
        <HBox alignment="CENTER_LEFT" spacing="10">
            <padding>
                <Insets top="20" right="20" bottom="20" left="20"/>
            </padding>
            <Label text="👤 My Profile"
                   style="-fx-text-fill: white; -fx-font-size: 26px; -fx-font-weight: bold;" />
            <Pane HBox.hgrow="ALWAYS" />
            <Button text="⬅ Back" onAction="#handleBack"
                    style="-fx-background-color: transparent; -fx-text-fill: white; -fx-font-size: 16;" />
        </HBox>
    </top>

    <!-- Center -->
    <center>
        <VBox alignment="CENTER" spacing="20">
            <padding>
                <Insets top="40"/>
            </padding>

            <Label text="Username: user123"
                   style="-fx-text-fill: white; -fx-font-size: 18;" />

            <Label text="Email: user@example.com"
                   style="-fx-text-fill: white; -fx-font-size: 18;" />

            <Button text="Edit Profile" onAction="#handleEditProfile"
                    style="-fx-background-color: white; -fx-font-size: 16; -fx-background-radius: 12;" />

            <Button text="Change Password" onAction="#handleChangePassword"
                    style="-fx-background-color: white; -fx-font-size: 16; -fx-background-radius: 12;" />

            <Button text="Delete Account" onAction="#handleDeleteAccount"
                    style="-fx-background-color: #e74c3c; -fx-text-fill: white; -fx-font-size: 16; -fx-background-radius: 12;" />
        </VBox>
    </center>
</BorderPane>